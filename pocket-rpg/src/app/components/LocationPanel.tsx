import React from 'react';import {useGame} from '../GameProvider';import {locations} from '../../content/locations';
export function LocationPanel(){const{state,commands}=useGame();const l=locations[state.world.locationId];return <section><h2>{l.label}</h2>{state.world.travel?<p>Переход...</p>:<div>{l.neighbors.map(id=><button key={id} onClick={()=>commands.travelTo(id)}>Перейти: {locations[id].label}</button>)}</div>}{l.enemies.length>0&&!state.combat&&<button onClick={commands.startEncounter}>Искать противника</button>}</section>}
