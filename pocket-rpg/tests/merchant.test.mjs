import test from 'node:test';import assert from 'node:assert/strict';import {buyItem,sellItem,decayPersonalMarketPressure} from '../dist/game-core/economy/merchant.js';
test('merchant stock is effectively unlimited',()=>{const s={gold:1000,inventory:{stacks:[]},pressure:{},pressureUpdatedAt:0};const b=buyItem(buyItem(s,'healing-potion',20,0,10),'healing-potion',20,0,10);assert.equal(b.inventory.stacks.find(x=>x.itemId==='healing-potion').quantity,40);});
test('immediate same city resale is a loss',()=>{const s={gold:1000,inventory:{stacks:[]},pressure:{},pressureUpdatedAt:0};const bought=buyItem(s,'healing-potion',1,0,100);const sold=sellItem(bought,'healing-potion',1,0,100);assert.ok(sold.gold<1000);});
test('buying quantity creates temporary 5 percent personal pressure',()=>{const s={gold:10000,inventory:{stacks:[]},pressure:{},pressureUpdatedAt:0};const bought=buyItem(s,'healing-potion',3,1000,100);assert.ok((bought.pressure['healing-potion']??0)>.1);const decayed=decayPersonalMarketPressure(bought,1000+6*60*60*1000);assert.ok(Math.abs(decayed.pressure['healing-potion']??0)<Math.abs(bought.pressure['healing-potion']));});
