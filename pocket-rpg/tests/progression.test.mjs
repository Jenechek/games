import test from 'node:test';import assert from 'node:assert/strict';import {xpRequiredForLevel,levelUpCharacter} from '../dist/game-core/progression/character.js';import {skillXpRequired,skillDepthMultiplier,addSkillXp} from '../dist/game-core/progression/skills.js';
test('character XP grows by 15 percent',()=>{assert.equal(xpRequiredForLevel(1000,1),1000);assert.equal(xpRequiredForLevel(1000,2),1150);});
test('level up adds exactly 15 whole attribute points with minimum one each',()=>{const before={level:1,xp:0,attributes:{strength:10,agility:10,intuition:10,vitality:10,wisdom:10,luck:10,charisma:10,intelligence:10}};const next=levelUpCharacter(before,{strength:40,agility:20,intuition:10,vitality:10,wisdom:5,luck:5,charisma:5,intelligence:5});const sum=o=>Object.values(o).reduce((a,b)=>a+b,0);assert.equal(sum(next.attributes)-sum(before.attributes),15);for(const v of Object.values(next.attributes))assert.ok(Number.isInteger(v)&&v>=11);});
test('skill XP uses 250 base and 15 percent growth',()=>{assert.equal(skillXpRequired(1),250);assert.equal(skillXpRequired(2),287.5);});
test('deeper skill nodes gain 65 percent more XP each depth',()=>{assert.equal(skillDepthMultiplier(0),1);assert.equal(skillDepthMultiplier(1),1.65);assert.ok(Math.abs(skillDepthMultiplier(2)-2.7225)<1e-10);assert.ok(Math.abs(skillDepthMultiplier(3)-4.492125)<1e-10);});
test('child skill may exceed parent but never exceeds 100',()=>{const parent=addSkillXp({level:20,xp:0},100);const child=addSkillXp({level:99,xp:skillXpRequired(100)-1},1e9);assert.ok(parent.level>=20);assert.equal(child.level,100);});
